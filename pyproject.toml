[project]
name = "vscode-jupyter-test"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "matplotlib",
    "ipywidgets",
    "ipympl",
    "ipykernel",
    "jupyter-server",
]
